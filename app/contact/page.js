import ContactComp from "@/components/layout/contact";

export const metadata = {
   title: "Contact",
   description:
      "Let's work together — get in touch for project inquiries or collaboration.",
   
};

export default function Contact() {
   return <ContactComp />;
}
